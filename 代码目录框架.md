# 灵境奇旅 · client 代码目录框架

> 配套 `项目骨架.md` 使用。技术选型建议见文末，目录以「按功能分层 + Aholo 3D / 业务层分离」为原则。

## 推荐技术栈
- Vite + React + TypeScript
- 路由：react-router
- 状态：zustand（Config 与 UserProgress 分离）
- 3D：three.js / @react-three/fiber（Aholo 暂为 Mock，3D 层做隔离接口，未来替换为真实 aHolo）
- 数据协议：`@hackday/contracts`（唯一来源）

## 目录树

```text
client/
├── index.html
├── package.json
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
├── .env.example
├── src/
│   ├── main.tsx                     # 入口：挂载 Provider + Router
│   ├── App.tsx                      # 路由壳
│   ├── router/
│   │   └── index.tsx                # 页面路由定义
│   │
│   ├── app/                         # 全局横切
│   │   ├── providers/
│   │   │   ├── ConfigProvider.tsx   # 公共景区配置 Provider
│   │   │   └── UserProgressProvider.tsx
│   │   └── stores/
│   │       ├── configStore.ts       # ScenicExperienceConfig + version 轮询
│   │       ├── userProgressStore.ts # UserProgress（与配置严格分离）
│   │       └── uiStore.ts           # 浮层/加载/定位等临时 UI 状态
│   │
│   ├── pages/                       # 页面级（对应项目骨架的模块）
│   │   ├── HomePage/                # 首页
│   │   ├── ScenicDetailPage/        # 景区详情页（西湖详情）
│   │   ├── SceneLoadingPage/        # 场景加载
│   │   ├── ExplorePage/             # 线上全景漫游（3D 探索，核心）
│   │   └── GuidePage/               # 线下智游向导（定位/AR，降级占位）
│   │
│   ├── features/                    # 业务功能模块（跨页面复用）
│   │   ├── hotspots/                # 热点系统（景点/游戏/传送 + 三段感知）
│   │   ├── story/                   # 景点故事浮层
│   │   ├── theme/                   # 主题切换（默认/中秋/国庆）
│   │   ├── minigame/                # 小游戏容器 + 猜灯谜
│   │   ├── reward/                  # 奖励/纪念卡
│   │   ├── assistant/               # 助手搭档（跟随/领路/提示）
│   │   ├── positioning/             # 定位 Provider + 模拟轨迹
│   │   ├── backpack/                # 背包（收集物/奖励）
│   │   └── avatar/                  # 我的 3D 形象（Mock）
│   │
│   ├── components/                  # 通用 UI
│   │   ├── HUD/                     # 品牌/场景、罗盘、任务条、主操作按钮
│   │   ├── overlays/                # 浮层/弹窗/结果页
│   │   └── common/                  # Button/Card/Toast 等
│   │
│   ├── three/                       # 3D 场景层（Aholo Mock 隔离层）
│   │   ├── SceneManager.ts          # 场景加载/渲染生命周期
│   │   ├── camera/                  # 第三人称跟随相机
│   │   ├── characters/              # 游客角色 + 助手角色
│   │   ├── collision/               # 碰撞/边界/出生点
│   │   ├── world/                   # 世界坐标热点、坐标映射
│   │   └── environment/             # 主题环境/氛围切换
│   │
│   ├── services/                    # 数据服务（仅公共配置接口）
│   │   ├── clientConfigService.ts   # GET /api/client/config + version 轮询
│   │   └── types.ts                 # 本地辅助类型（模型仍从 contracts 导入）
│   │
│   ├── lib/                         # 工具
│   │   ├── id.ts                    # WEST_LAKE_IDS 导入/封装
│   │   ├── distance.ts              # 热点距离计算
│   │   └── theme.ts                 # 主题应用/回退
│   │
│   └── styles/                      # 全局样式 / 主题变量
│       ├── global.css
│       └── tokens.css
│
└── public/
    └── assets/                      # 静态资源（纪念卡、占位图等）
```

## 分层约定

### 1. 3D 层（`src/three/`）
- 只负责：场景加载渲染、第三人称相机、游客/助手角色、碰撞边界、出生点、世界坐标热点、主题环境切换。
- 向上通过接口回传：加载状态、坐标、朝向、热点状态。
- **不写业务**：不读配置、不判断热点类型、不处理奖励。
- 未来 aHolo 接入时只替换这一层实现，业务层不动。

### 2. 业务层（`src/features/` + `src/pages/` + `src/app/`）
- 只负责：页面导航、HUD、浮层、按钮、定位 Provider、坐标映射、模拟轨迹、景点/游戏/奖励/进度、配置与发布联动。
- 数据一律来自 `clientConfigService` + `configStore`，不直连 3D 层内部。

### 3. 状态边界（重点）
- `configStore`：公共景区配置（`ScenicExperienceConfig`），只被 `version` 变化驱动更新。
- `userProgressStore`：游客进度（完成互动、已解锁奖励、位置、场景、任务状态），管理端发布**不得重置**。
- 两者永远不互相写入。

## 数据流

```text
GET /api/client/config（每 2s 轮询）
  → version 变化 → configStore 更新
  → applyTheme(activeThemeId)
  → renderHotspots(enabled === true)
  → three/SceneManager 应用主题环境
  → 用户操作 → userProgressStore（独立持久化）
```

## 关键约束复述
- 只调用 `/api/client/config`，禁止 `/api/admin/*`。
- 类型/ID 从 `@hackday/contracts` 导入，禁止自建模型。
- 玩法用 `miniGameId` 查 `miniGames`；奖励用 `rewardId` 查 `rewards`；三者不混。
- 默认无移动轮盘；轮盘/键盘仅作定位失败/调试降级。

## 落地顺序（建议）
1. 先建 `router + pages` 页面壳和 `styles`，跑通路由。
2. 建 `services/clientConfigService` + `configStore` + `userProgressStore`。
3. 建 `three/` 最小场景 + 第三人称相机 + 游客/助手 + 模拟轨迹。
4. 依次接 `hotspots → story → theme → minigame → reward → backpack`。
5. 最后接管理端联动与降级路径。
