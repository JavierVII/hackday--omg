# 管理端环境变量模板 —— 复制为 .env.development 后按需修改。
# 所有变量在代码里都有默认值，不配 .env 也能跑（默认纯前端 Mock）。

# mock | api —— 不配默认 mock（无后端，配置/资产走 localStorage 展示）
VITE_CONFIG_MODE=mock

# 管理端路由与 vite 的 base 路径（默认 /admin）
VITE_ADMIN_BASE_PATH=/admin

# 接真实后端时的 API 前缀（默认空，即同源 /api/admin/*）
VITE_CONFIG_API_URL=
