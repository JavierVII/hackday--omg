import { useEffect, useState } from "react"; import type { Asset3D } from "@hackday/contracts"; import { Check, Circle, Cpu, Sparkles } from "lucide-react"; import { useNavigate, useParams } from "react-router-dom"; import { AssetHeader } from "../../components/assets/AssetHeader"; import { AssetStepper } from "../../components/assets/AssetStepper"; import { Button, Card, Loading } from "../../components/ui"; import type { ReconstructionJob } from "../../services/aholo"; import { assetService } from "../../services/assets/assetService"; import { useToast } from "../../store/ToastProvider";
const stages = [{ key: "uploaded", label: "等待重建", at: 0 }, { key: "analyzing", label: "素材预处理中", at: 20 }, { key: "geometry", label: "AI 重建中", at: 45 }, { key: "texture", label: "纹理生成", at: 70 }, { key: "optimizing", label: "模型优化", at: 90 }, { key: "completed", label: "完成", at: 100 }];
export function AssetBuildPage() { const { id = "" } = useParams(), navigate = useNavigate(), { showToast } = useToast(); const [asset, setAsset] = useState<Asset3D>(), [job, setJob] = useState<ReconstructionJob>(), [error, setError] = useState(""); useEffect(() => { let active = true, timer: number | undefined; const poll = async () => { try { const next = await assetService.syncReconstruction(id); if (!active) return; setAsset(next.asset); setJob(next.job); if (next.asset.status === "reconstructing") timer = window.setTimeout(poll, 2500); else if (next.asset.status === "pending_review") showToast("AI 重建已完成"); } catch (reason) { if (active) setError(reason instanceof Error ? reason.message : "重建任务加载失败"); } }; poll(); return () => { active = false; if (timer) window.clearTimeout(timer); }; }, [id, showToast]); if (error) return <div className="page detail-page"><AssetHeader title="AI 重建"/><Card className="error-card">{error}</Card></div>; if (!asset || !job) return <div className="page detail-page"><AssetHeader title="AI 重建"/><Loading label="正在读取真实重建任务"/></div>; return <div className="page detail-page"><AssetHeader title="AI 重建"/><AssetStepper current={2}/><Card className="build-hero"><div className="ai-orbit"><Cpu size={34}/><span/></div><p>AHOLO 3DGS RECONSTRUCTION</p><h2>{asset.name}</h2><strong>{job.progress}%</strong><div className="progress-track"><i style={{ width: `${job.progress}%` }}/></div><span>{job.message}</span></Card><section className="build-stages">{stages.map(stage => { const done = job.progress >= stage.at && stage.at !== 100 || job.progress === 100; return <div className={`${done ? "done" : ""} ${stage.key === job.stage ? "current" : ""}`} key={stage.key}><span>{done ? <Check size={14}/> : <Circle size={10}/>}</span><strong>{stage.label}</strong></div>; })}</section>{asset.status === "pending_review" && <Button className="full-button sticky-action" onClick={() => navigate(`/assets/${id}/review`)}><Sparkles size={18}/>查看重建结果</Button>}{asset.status === "failed" && <Card className="error-card">{asset.failureReason ?? "aHolo 重建失败"}</Card>}</div>; }
